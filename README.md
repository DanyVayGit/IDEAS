This branch implements the creation of its own database for studying principles and algorithms.

command_get = 'python -m dbdb.tool base.db get test'

command_set = 'python -m dbdb.tool base.db set test test'
